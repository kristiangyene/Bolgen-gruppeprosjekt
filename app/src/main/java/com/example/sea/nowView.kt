package com.example.sea

import android.content.Intent
import android.view.MenuItem
import com.google.android.gms.location.LocationRequest

/*
interface nowView {
    fun checkPermission(permissionOption: String): Boolean
    fun requestPermission(permissionOption: String)
    //fun onRequestPermissionsResult(requestCode: Int, permissions: Array<String>, grantResults: IntArray)
    fun createLocationRequest()
    fun getLocation(locationRequest: LocationRequest)
    fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?)
    fun stopUpdate()
    fun onPause()
    fun onResume()
    fun updateTextViewStart(position: Int)
    fun updateTextView(position: Int)
    fun onBackPressed()
    fun dialog(menuItem: MenuItem, checkedItems: BooleanArray)
    fun firstStart(): Boolean
}

*/